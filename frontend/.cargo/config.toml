[target.wasm32-unknown-unknown]
# wasm-bindgen 0.2.121 CLI fails with "failed to find intrinsics to enable
# clone_ref function" on Rust 1.82+ unless reference-types are disabled here.
# The CSP (not this flag) was the cause of the blank page.
rustflags = ["-C", "target-feature=-reference-types"]
