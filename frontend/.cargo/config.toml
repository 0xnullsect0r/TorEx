# No rustflags needed for wasm32-unknown-unknown.
# wasm-bindgen-cli 0.2.121 (installed in nginx/Dockerfile) handles Rust 1.82+
# reference-types correctly. The old -C target-feature=-reference-types flag
# was disabling reference-types, which caused the JS glue to crash at runtime
# (__wbindgen_externref_table_dealloc export missing) → blank/black page.
