global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: torex_backend
    static_configs:
      - targets: ["127.0.0.1:8080"]
    metrics_path: /metrics
    # NOTE: /metrics is NOT proxied by nginx externally
